# Design system — moved

The living design-system package is [`design-system/`](../../design-system/) at the repo root
(`design_tokens.json` is the single token source; `rn_tokens.ts` + `tailwind.*.js` derive from it —
the copies that used to sit here were stale snapshots and have been removed).

- Principles, component contract, parity: [`docs/DESIGN_SYSTEM.md`](../DESIGN_SYSTEM.md)
- Glass Studio v2 visual spec: [`docs/redesign/`](../redesign/README.md)
