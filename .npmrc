# Workspaces + Expo Router + multiple React consumers: npm's strict peer
# resolution fails on expo-router/react-server-dom-webpack vs hoisted react.
# Same tree is sound at runtime; lockfile is the source of truth after install.
legacy-peer-deps=true
