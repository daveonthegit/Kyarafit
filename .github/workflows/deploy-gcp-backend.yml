name: Deploy Backend to GCP Cloud Run

# The Go backend has been archived. This workflow is disabled.
# The application backend is now Convex (auto-deployed via npx convex deploy).

on:
  workflow_dispatch: # Only run manually if needed

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - name: Skip — backend archived
        run: |
          echo "## ⚠️ Backend Deployment Skipped" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The Go Fiber backend has been archived." >> $GITHUB_STEP_SUMMARY
          echo "The backend is now **Convex** — deploy via \`npx convex deploy\` or the Convex GitHub Action." >> $GITHUB_STEP_SUMMARY
